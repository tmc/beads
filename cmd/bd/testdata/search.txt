[!dolt] skip
# Test bd search text and filter behavior
bd init --prefix test --quiet
bd create 'Login authentication bug' --description 'Users cannot login with OAuth' --type bug --priority 1
bd create 'Database refactor task' --description 'Improve schema performance' --type task --priority 3
bd create 'User profile feature' --description 'Ship profile editing' --type feature

bd search authentication
stdout 'Login authentication bug'

bd search task --status open --type task
stdout 'Database refactor task'

bd search nonexistent-term-xyz
! stdout 'Login'
! stdout 'Database'

! bd search --type task
stderr 'search query is required'
