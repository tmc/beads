[!dolt] skip
# Test bd close error handling
bd init --prefix test --quiet

! bd close nonexistent-id
stderr 'not found|no issue found|Error fetching'

! bd close
stderr 'at least one issue ID|issue ID'
