[!dolt] skip
# Test bd delete validation failures
bd init --prefix test --quiet

! bd delete
stderr 'at least one issue ID|issue ID'

! bd delete nonexistent-id --force
stderr 'not found|no issues found'
