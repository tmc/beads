[!dolt] skip
# Test bd dep validation failures
bd init --prefix test --quiet
bd create 'First issue' --description 'Dependency source' --json
extractid FIRST

! bd dep add $FIRST nonexistent-id
stderr 'not found|no issue found'

! bd dep add $FIRST $FIRST
stderr 'cannot depend on itself|cycle'

! bd dep tree nonexistent-id
stderr 'not found|no issue found'
