[!dolt] skip
# Test bd dep tree renders the dependency graph
bd init --prefix test --quiet
bd create 'Root issue' --description 'Tree root' --json
extractid ROOT
bd create 'Child issue' --description 'Tree child' --json
extractid CHILD

bd dep add $CHILD $ROOT

bd dep tree $ROOT
stdout 'Root issue'
stdout 'Child issue'
