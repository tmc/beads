[!dolt] skip
# Test bd show error handling
bd init --prefix test --quiet

! bd show
stderr 'at least one issue ID is required|issue ID'

! bd show nonexistent-id
stderr 'not found|no issues found|Error fetching'
